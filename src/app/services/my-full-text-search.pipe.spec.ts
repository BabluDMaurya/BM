import { MyFullTextSearchPipe } from './my-full-text-search.pipe';

describe('MyFullTextSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MyFullTextSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
